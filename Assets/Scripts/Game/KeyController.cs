using UnityEngine;

public class KeyController : MonoBehaviour
{
    
    void Update()
    {
        /*var cr = Game.Get.CurrentCreature;
        if (Input.GetKeyDown(KeyCode.A) && Interface.Get.AttackButton.interactable)
        {
            cr.Attack();
        }
        if (Input.GetKeyDown(KeyCode.S) && Interface.Get.MoveButton.interactable)
        {
            cr.MoveForward();
        }
        if (Input.GetKeyDown(KeyCode.D) && Interface.Get.NextMoveButton.interactable)
        {
            Game.Get.NextMove();
        }
        if (Input.GetKeyDown(KeyCode.F) && Interface.Get.NextMoveButton.interactable)
        {
            Interface.Get.ChangeCreature();
        }*/
    }

}